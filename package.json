{
  "name": "stacks-v3",
  "private": true,
  "version": "0.2.1",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "echo 'use: docker compose up -d --build --wait' && exit 1",
    "verify": "node scripts/check-boundaries.mjs && node --test 'scripts/*.test.mjs' && pnpm -r run typecheck && pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test"
  }
}
