Task 1 Compose foundation verification

Commands run successfully:
- docker compose config
- python3 -m py_compile apps/api/app/main.py
- python3 -m py_compile apps/worker/worker.py
- npm run build in apps/web
- docker compose up --build -d
- docker compose up --build -d --remove-orphans
- curl -fsS http://localhost:8000/health
- curl -fsS http://localhost:5174

API health response:
{"status":"ok"}

Web response:
Dockerized Vite app shell served from http://localhost:5174.

Compose services verified after orphan cleanup:
- api: healthy, host port 8000
- web: running, host port 5174
- worker: running
- postgres: healthy
- qdrant: healthy

Notes:
- Existing container `portainer` was using host port 8000 and was stopped to satisfy the required API health URL.
- Existing orphan containers for the old Compose project were removed with `docker compose up --build -d --remove-orphans`.
- Local Python LSP diagnostics could not run because basedpyright and pip are unavailable in this environment; Python source was validated with py_compile and container runtime health.
