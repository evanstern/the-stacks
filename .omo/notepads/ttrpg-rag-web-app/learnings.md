
- 2026-05-31 Task 11: HTML parser now strips obvious boilerplate tags (nav/header/footer/script/style/etc.), preserves title/heading metadata, and emits parser warnings into ingestion job metadata/events. EPUB support is pragmatic zip/XHTML extraction with at least one readable chunk from the fixture, without introducing Calibre or other complex ebook tooling.

- 2026-05-31 Worker ingestion slice: The live worker calls the pre-embedding `process_next_queued_job()` path so queued uploads claim transactionally, parse/chunk into canonical `sources/documents/sections/chunks`, emit durable `queued`, `processing`, `chunking_started`, `chunking_completed`, and `awaiting_embedding` events, and then stop at `awaiting_embedding`; embedding/indexing remains available through explicit API ingestion functions for the next wave, not the worker loop.

- 2026-06-01 Task 9 web observability follow-up: The RR7 upload surface should treat `awaiting_embedding` as a settled local-dev outcome alongside `completed`/error states because the worker intentionally stops after chunking. Browser QA on host `5174` verified admin login, `sample.md` upload to `awaiting_embedding`, `/records` showing upload/job events/retrieval/source/chunk preview data, and `sample.pdf` surfacing the exact FastAPI 415 detail (`Unsupported file type. Supported types: EPUB, HTML, TXT, MD.`).

- 2026-06-01 DDB archive frontend precursor for plan Tasks 9/11: Requested `.omo/notepads/ddb-archive-source-viewer/*.md` directory was absent, so this evidence was appended to the existing web-app learnings file. Inspected `main/.omo/plans/ddb-saved-html-import.md` Task 9/11 and the frontend files `apps/web/app/routes/upload.tsx`, `apps/web/app/routes/records.tsx`, `apps/web/app/routes/chat.tsx`, `apps/web/app/lib/api.ts`, `apps/web/app/router.tsx`, `apps/web/app/components/app/app-shell.tsx`, `apps/web/app/components/app/top-nav.tsx`, `apps/web/src/main.tsx`, and `apps/web/src/styles.css`. Upload archive UI can extend `UploadRoute`: it already accepts `.html/.htm`, posts through shared `uploadFile()`, polls `getIngestionJob()`/`getJobEvents()`, and treats `awaiting_embedding` as terminal; likely add DDB/archive copy and metadata/event display in-place rather than a new upload endpoint/component. Records can extend inline detail helpers in `records.tsx`: `JobMeta`, `SourceMeta`, and `ChunksSection` currently show metadata keys only, so DDB fields such as `source_type`, `section_path`, `heading_id`, `content_chunk_ids`, `raw_sha256`, `rendered_html_path`, and `citation_anchor` need explicit rendering if records observability is part of the UI task. Chat archive citation viewer should extend or extract the inline citation path in `chat.tsx` (`MessageText`, `CitationMarkerGroup`, `CitationMarker`, `CitationRail`, `CitationCard`, `citationMetadata()`); current cards show source/title/location/excerpt and IDs only, so a sandboxed iframe viewer without `allow-scripts` is best added as a new reusable child component under the citation card/rail flow once API metadata exposes a safe rendered artifact URL/path plus anchor. Shared API types in `app/lib/api.ts` are currently generic `Record<string, unknown>` metadata for jobs/events/chunks/citations, so future work can either add narrow DDB metadata helper types/guards there or local guards in `chat.tsx`/`records.tsx`; no new route wiring is needed unless an artifact-serving endpoint requires a client helper. Styling entrypoint is `src/styles.css`, with existing citation classes `.citation-rail`, `.citation-card`, `.citation-body`, `.citation-excerpt`, and records classes `.records-*`; iframe viewer styles should live there. LSP diagnostics on candidate files reported dependency-resolution noise for TSX files (`react`, `react-router`, `lucide-react`, `react-markdown`, `react/jsx-runtime` not resolved), while `apps/web/app/lib/api.ts` and `apps/web/src/styles.css` reported no diagnostics.
