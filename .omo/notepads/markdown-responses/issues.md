## 2026-06-01

- `lsp_diagnostics` could not run in this environment because the configured `basedpyright` server is not installed (`basedpyright-langserver` missing). The code change itself verified through `make test`, which passed.
