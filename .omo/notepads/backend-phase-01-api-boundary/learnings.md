- 2026-06-07: R3 is now captured as the docs-only `specs/005-public-api-error-mapping-convention/` package. The requested notepad path did not exist when I checked it, so this note records the missing-path result and the naming decision together. The package is anchored in current repo behavior first: `auth.py`, `routes_sessions.py`, `routes_uploads.py`, `routes_ingestion.py`, `routes_records.py`, and `routes_archives.py` show the public `400`, `401`, `404`, `413`, `415`, and safe server-failure shapes that the spec documents.
- 2026-06-08: R4 is now captured as the docs-only `specs/006-chat-facade-cleanup-or-naming-decision/` package. The missing-path check pattern stays the same, and this note keeps the naming decision tied to observed behavior: `chat_rag.py` is still the compatibility facade, `chat_session_service.py` owns orchestration, `routes_sessions.py` is thin, and the public chat boundary stays `404` for missing sessions with safe `503` failures.
- 2026-06-08: 003 landed as the code-first session-message boundary slice: `POST /sessions/{session_id}/messages` now maps a named `SessionMessageNotFoundError` from `chat_session_service.py` to the stable public `404 Session not found`, while route tests use `TestClient(app)` plus `_chat_session_service_dependency`/collaborator overrides to keep the boundary isolated and preserve `ChatMessageEnvelope`.
- 2026-06-08: 004 landed as the code-first upload-intake boundary slice: `POST /uploads` now adapts multipart files into `UploadIntakeFile` DTOs and delegates orchestration to `upload_intake_service.py` through `_upload_intake_service_dependency`, while `routes_uploads.py` keeps public error/status mapping and the existing `archive_storage.py`, `archive_repair.py`, and `ingestion.py` seams remain live.
- 2026-06-08: Roadmap wording was reclassified so R2 and R4 read like implementation follow-ups while R1, R3, R5, R6, and R7 stay honest as reference or guardrail items. The roadmap now says that directly instead of quietly defaulting to docs-only phrasing when runtime hardening is the real follow-up.
- 2026-06-08: R4 is now an actual code rename: `apps/api/app/chat_rag.py` moved to `apps/api/app/chat_session_rag.py`, app/Alembic/tests import the new module directly, and no compatibility shim was needed because backend search found no remaining internal `chat_rag` consumers after migration.
