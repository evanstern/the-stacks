{
  "name": "@stacks/ingestion",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@stacks/core": "workspace:*",
    "@stacks/db": "workspace:*",
    "@stacks/ingestion-contract": "workspace:*",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@stacks/ingestion-plugins": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
